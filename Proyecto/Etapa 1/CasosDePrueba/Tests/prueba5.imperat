{
    int max_, _;
    function[..5] F; 

    max_ := F.0;
    _ := 1;
    while _ <= 5  -->
       if max_ < F._ -->
          max_ := F._
       [] max_ >= F._ -->
          skip
       fi;
       _ := _ + 1;
    end
    // Esto es un comentario. Debe ser ignorado.
} 