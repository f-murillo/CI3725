{
    function[..2] _A;
    int b;

    // ejemplo de asignacion de lista a un objeto que no es funcion
    b := 1, 3, 4;
    print "First: " + _A.0 + "\nSecond: " + _A.1 + "\nThird: " + _A.2
    // Esto es un comentario. Debe ser ignorado.
} 