{
    int x;
    int y;
    int z;

    x := 10;

    if x > 5 -->
        y := 20;
        
        if y == 20 --> { int a; a:= 82; x:=a };
            z := 30
        fi;

        x := x + 1
    fi
}