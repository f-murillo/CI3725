{
    int i, max;
    bool itIs;
    function[..5] A;

    itIs := false;
    i := 0;
    while i <= 5 and !itIs -->
       itIs := A.i == 10;
       i := i+1
    end
    // Esto es un comentario. Debe ser ignorado.
}